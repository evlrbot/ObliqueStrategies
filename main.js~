if (Meteor.isClient) {
    Template.card.events = {
	"click .card" : function() {
	    //alert('h3llo');
	    $(".card").flip({
		direction:'tb'
	    })
	}
    };
}

if (Meteor.isServer) {
  Meteor.startup(function () {
    // code to run on server at startup
  });
}
